# Agents (packages/guidance-agents/src/agents)
Contains the core reasoning loops and orchestration logic (using PocketFlow).
Each sub-folder here represents an independent intelligent agent or workflow, such as 	ask_decomposition.

**Rule:** Do not put raw API calls or SQL queries here. Use the 	ools or services modules instead.
